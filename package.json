{
  "name": "mams-unified",
  "version": "1.0.0",
  "description": "Unified Military Asset Management System for deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix smart/client && npm run build --prefix smart/client && npx prisma generate",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^7.7.0",
    "@prisma/client": "5.22.0",
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.9.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "5.22.0"
  }
}
